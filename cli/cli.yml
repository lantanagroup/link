cli:
  generate-and-submit:
    bundleIds:
  query:
    data-store:
      base-url:
      username:
      password:
    fhir-server-base:
    uscore:
      fhir-server-base:

  refresh-patient-list:
    api-url:
    auth:
      token-url:
      client-id:
      client-secret:
      user:
      pass:
      scope:
      credential-mode:
    patient-list:
      - patient-list-path:
        census-identifier:
          -
uscore:
  fhir-server-base:

query:
  auth-class:
  query-class:
  fhir-server-base:
  auth:
    epic:
      token-url:
      audience:
      client-id:
      key: